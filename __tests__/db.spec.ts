describe('dbExec', () => {
  it.todo('rejects on unsuccessful DB connection')
  it.todo('rejects on unsuccessful DB query exec and releases the DB connection')
  it.todo('resolves on successful DB query exec and releases the DB connection')
})
